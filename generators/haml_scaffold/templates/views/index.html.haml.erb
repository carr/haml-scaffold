%h2.items_title Listing <%= plural_name.humanize.downcase %>
%ul.items_menu
  %li.new
    = link_to 'New <%= singular_name.humanize.downcase %>', new_<%= singular_name %>_path
= render :partial => 'list', :locals => {:<%= plural_name %> => @<%= plural_name %>}

